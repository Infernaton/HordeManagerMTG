const cardModel = {
    id: Number,
    name: String,
    mana_cost: String,
    type_line: String,
    oracle_text: String,
    power: String,
    toughness: String,

    full_image: URL
}

export default cardModel;